---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./secrets.sops.yaml
  - ./helmrelease.yaml

configMapGenerator:
  - name: configarr-configmap
    files:
      - config/config.yml

generatorOptions:
  disableNameSuffixHash: true
