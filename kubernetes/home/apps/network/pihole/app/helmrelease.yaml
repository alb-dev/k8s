---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pihole
spec:
  chartRef:
    kind: OCIRepository
    name: pihole
  interval: 30m
  values:
    controllers:
      pihole:
        replicas: 1
        strategy: RollingUpdate

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          pihole:
            image:
              repository: ghcr.io/pi-hole/pihole
              tag: 2026.07.1@sha256:3dd9cd9e41a01d103cf7aafb59606b1cf464bd1e8782b20320955f7341717578
            env:
              TZ: ${TIMEZONE}
              FTLCONF_dns_listeningMode: all
              FTLCONF_webserver_port: &pihole-port 80
              FTLCONF_dns_upstreams: 127.0.0.1#5335
              FTLCONF_dns_dnssec: true
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            securityContext:
              allowPrivilegeEscalation: false
            resources:
              requests:
                cpu: 100m
              limits:
                # looking at the query log can spike the memory usage very high
                memory: 512Mi
          unbound:
            image:
              repository: alpinelinux/unbound
              tag: latest@sha256:8b845cc18fc0f327d5ff4a8471aa6e505a4f9e3f242b73817cda97314afc6e73
            securityContext:
              allowPrivilegeEscalation: false
            resources:
              requests:
                cpu: 100m
              limits:
                memory: 512Mi

    service:
      app:
        forceRename: pihole
        controller: pihole
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 192.168.178.3
        ports:
          dns-udp:
            port: 53
            protocol: UDP
          dns-tcp:
            port: 53
            protocol: TCP
          http:
            primary: true
            port: *pihole-port
            protocol: HTTP
          https:
            port: 443
            protocol: HTTPS

    route:
      app:
        hostnames:
          - '{{ .Release.Name }}.${CLUSTER_DOMAIN}'
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - name: pihole
                port: 80
        #rules:
        #  - backendRefs: [{ identifier: app }]
        #    sessionPersistence:
        #      sessionName: pihole
        #      type: Cookie
        #      absoluteTimeout: 35m

    persistence:
      config:
        existingClaim: ${VOLSYNC_CLAIM}
        advancedMounts:
          pihole:
            pihole:
              - path: "/etc/pihole"
                subPath: "pihole"
      unbound-config:
        type: configMap
        name: unbound-conf
        advancedMounts:
          pihole:
            unbound:
              - path: "/etc/unbound/unbound.conf"
                subPath: "unbound.conf"
      root-hints:
        type: configMap
        name: root-hints
        advancedMounts:
          pihole:
            unbound:
              - path: "/var/lib/unbound/root.hints"
                subPath: "root.hints"                