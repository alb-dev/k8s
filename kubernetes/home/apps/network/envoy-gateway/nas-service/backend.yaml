---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: nas-service
spec:
  endpoints:
    - ip:
        address: "${SECRET_MEDIA_SERVER}"
        port: 5000
  #tls:
  #  insecureSkipVerify: true