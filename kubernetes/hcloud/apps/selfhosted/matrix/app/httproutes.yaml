---
# HTTP Route for Synapse (Matrix homeserver)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-synapse
  namespace: matrix
spec:
  hostnames: ["matrix.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-synapse
          namespace: matrix
          port: 8008

---
# HTTP Route for Matrix Authentication Service (MAS)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-auth-service
  namespace: matrix
spec:
  hostnames: ["mas.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-matrix-authentication-service
          namespace: matrix
          port: 8080

---
# HTTP Route for Matrix RTC (Real-Time Communication)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-rtc
  namespace: matrix
spec:
  hostnames: ["rtc.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /sfu/get
      backendRefs:
        - name: matrix-matrix-rtc-authorisation-service
          namespace: matrix
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-matrix-rtc-sfu
          namespace: matrix
          port: 7880

---
# HTTP Route for Element Admin
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-admin
  namespace: matrix
spec:
  hostnames: ["admin-matrix.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-element-admin
          namespace: matrix
          port: 8080

---
# HTTP Route for Element Web
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-element
  namespace: matrix
spec:
  hostnames: ["chat.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-element-web
          namespace: matrix
          port: 80

---
# HTTP Route for Well-Known Delegation
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-well-known
  namespace: matrix
spec:
  hostnames: ["matrix.albdev.cloud"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /.well-known/
      backendRefs:
        - name: matrix-well-known
          namespace: matrix
          port: 8010
