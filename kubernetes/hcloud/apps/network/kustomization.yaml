---

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./smtp-relay/ks.yaml
  - ./tailscale/ks.yaml
  - ./envoy-gateway/ks.yaml
  - ./traefik/ks.yaml